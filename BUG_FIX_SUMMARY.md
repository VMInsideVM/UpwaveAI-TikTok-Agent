# Agent 问题修复总结

## 问题分析（基于 LangSmith Trace）

### 问题 1: 智能推荐后没有询问用户是否应用

**问题现象**:
- Agent 给出智能推荐："建议选 `female`（女粉为主）"、"建议选 `25-34`" 等
- 但只说"您可以直接确认当前参数，或告诉我要添加/修改什么"
- **用户不知道如何应用这些推荐**

**根本原因**:
- 推荐话术不够明确，缺少具体的应用指引
- 用户不知道应该输入什么来应用推荐

**修复方案** ([agent.py:155-166](agent.py#L155-L166)):
```python
# 新的推荐格式（必须明确提供应用方式）
"💡 **针对您的商品（XXX）的智能推荐**：
1. **粉丝性别**：建议选 `female`（女粉为主）→ ✅ 美妆香水的核心消费群体
2. **粉丝年龄**：建议选 `25-34` → ✅ 该年龄段女性是香水主力购买人群
3. **认证类型**：建议选 `verified` → ✅ 奢侈品推荐已认证达人提升信任度

---

**如何应用这些推荐**：
• 如果接受推荐，请回复：**'应用推荐'** 或 **'添加这些条件'**
• 如果只想添加部分条件，请告诉我具体要添加哪些
• 如果不需要，直接回复 **'确认当前参数'** 继续下一步"
```

**改进点**:
1. ✅ 明确列出推荐的参数和理由
2. ✅ 提供清晰的应用指引（三个选项）
3. ✅ 使用加粗强调关键操作词

---

### 问题 2: 用户咨询问题后，Agent 错误地推进到排序选择阶段

**问题现象**:
```
用户: "美国，粉丝数10w-20w，找两个达人"
Agent: [调用 review_parameters 展示参数摘要] → "⚠️ **请您确认以上参数是否满意？**"

用户: "账号类型有哪几种，认证类型是什么，带货达人是什么"  ← 咨询性提问
Agent: [回答问题] + [给出智能推荐]
       ❌ 然后调用了 get_max_page_number 和 analyze_quantity_gap
       ❌ 输出："您之前已确认筛选参数，现在需要 **选择排序方式**（1-6）"
```

**根本原因**:
1. Agent 判断这是咨询性提问 ✅（正确）
2. Agent 回答了问题并给出推荐 ✅（正确）
3. **Bug**: Agent 在回答咨询问题的同时，**错误地调用了 `get_max_page_number` 和 `analyze_quantity_gap`**
4. 导致流程自动推进到了排序选择阶段，**用户根本没有确认参数**

**修复方案**:

#### 1. 强化 System Prompt 防御机制 ([agent.py:148-171](agent.py#L148-L171))

```python
1️⃣ **咨询性提问** (Informational Questions):
   - **处理方式**:
     * 直接从知识库或已有信息中回答
     * **⛔ 绝对禁止调用任何工具**（不要调用 record_user_needs、match_product_category、get_max_page_number、analyze_quantity_gap 等）
     * 回答后**根据对话历史判断当前阶段**，提醒用户继续：
       - 如果刚展示了参数摘要（有"请您确认以上参数是否满意"）→ **⛔ 绝对不要调用任何工具，只回答问题并等待用户确认或修改参数**
     * **🚨 重要**：在参数确认阶段（review_parameters 之后），只有当用户明确说"确认"、"好的"、"可以"、"应用推荐"等确认词时，才能调用 get_max_page_number 等工具推进流程。否则必须停留在参数确认阶段
```

#### 2. 更新参数确认流程规则 ([agent.py:198-220](agent.py#L198-L220))

```python
4. **【强制】参数确认循环** - ⚠️ 此步骤必须执行，不可跳过:
   - **🚨 进入参数确认等待状态**:
     * **如果用户提出咨询性问题**（例如"账号类型有哪几种"、"认证类型是什么"）：
       - **⛔ 绝对不要调用任何工具**（特别是 get_max_page_number、analyze_quantity_gap）
       - 只回答问题并给出推荐
       - **继续等待用户确认或修改参数**
       - 不要自动推进到步骤 5
     * **只有在用户明确表示满意时才继续**:
       - 中文确认词: "好的"、"可以"、"没问题"、"行"、"开始"、"确认"、"就这样"、"ok"、"确认当前参数"、"应用推荐"、"添加这些条件"
       - **只有出现这些确认词，才能进入步骤 5**
   - **🔥 关键规则**: 在参数确认阶段（review_parameters 之后），**除非用户明确确认**，否则**绝对不允许调用 get_max_page_number 或任何后续工具**
```

#### 3. 添加重要规则防御 ([agent.py:308-343](agent.py#L308-L343))

```python
## 重要规则:
- **🔥 智能处理临时提问**:
  * **如果用户在流程中提出咨询性问题**：
    - **⛔ 绝对禁止调用任何工具**（特别是不要调用 get_max_page_number、analyze_quantity_gap 等）
    - **回答后，查看对话历史判断当前阶段**：
      * 如果最后的工具调用是 review_parameters → **⛔ 绝对不要调用 get_max_page_number 等工具，必须等待用户明确确认**
    - **🚨 关键防御机制**：如果发现自己在回答咨询问题的同时调用了 get_max_page_number 或 analyze_quantity_gap，立即停止，这是错误的行为
  * **判断是否为咨询性提问的关键特征**：
    - 包含疑问词：什么、哪些、怎么、为什么、有哪几种
    - 不包含确认词：确认、好的、可以、应用推荐等

- **🛑 参数确认阶段防御**: 在 review_parameters 之后，**只有检测到用户明确确认**（"好的"、"确认"、"应用推荐"等），才允许调用 get_max_page_number。用户提问时绝对禁止推进流程
```

#### 4. 添加状态追踪 ([agent.py:44](agent.py#L44))

```python
self.waiting_for_param_confirmation = False  # 🆕 是否正在等待参数确认
```

---

## 修复后的预期行为

### 场景 1: 智能推荐应用流程

```
用户: "美国，粉丝数10w-20w，找两个达人"
Agent: [调用 review_parameters]
       📋 **当前筛选参数摘要**
       [参数列表...]
       ⚠️ **请您确认以上参数是否满意？**

用户: "账号类型有哪几种，认证类型是什么，带货达人是什么"
Agent: ✅ 正确回答：
       💡 **关于您询问的参数说明**：
       1️⃣ **账号类型 (account_type)**
       [详细说明...]

       💡 **针对您的商品（Miss Dior香水）的智能推荐**：
       1. **粉丝性别**：建议选 `female`（女粉为主）→ ✅ 美妆香水的核心消费群体
       2. **粉丝年龄**：建议选 `25-34` → ✅ 该年龄段女性是香水主力购买人群
       3. **认证类型**：建议选 `verified` → ✅ 奢侈品推荐已认证达人提升信任度

       ---

       **如何应用这些推荐**：
       • 如果接受推荐，请回复：**'应用推荐'** 或 **'添加这些条件'**
       • 如果只想添加部分条件，请告诉我具体要添加哪些
       • 如果不需要，直接回复 **'确认当前参数'** 继续下一步

       ❌ 不会调用 get_max_page_number
       ❌ 不会推进到排序选择阶段
       ✅ 继续等待用户确认或修改参数

用户: "应用推荐"
Agent: [调用 update_parameter 添加推荐的条件]
       [调用 review_parameters 再次展示更新后的参数]
       ⚠️ **请您确认以上参数是否满意？**

用户: "确认"
Agent: ✅ 现在才调用 get_max_page_number 和 analyze_quantity_gap
       ✅ 进入排序选择阶段
```

### 场景 2: 直接确认参数

```
用户: "美国，粉丝数10w-20w，找两个达人"
Agent: [调用 review_parameters]
       📋 **当前筛选参数摘要**
       ⚠️ **请您确认以上参数是否满意？**

用户: "好的"
Agent: ✅ 调用 get_max_page_number 和 analyze_quantity_gap
       ✅ 进入排序选择阶段
```

---

## 测试建议

### 测试用例 1: 咨询后应用推荐
```python
# 输入序列
inputs = [
    "美国，粉丝数10w-20w，找两个达人",
    "账号类型有哪几种，认证类型是什么，带货达人是什么",
    "应用推荐",
    "确认"
]

# 预期行为
# 第1轮：展示参数摘要，等待确认
# 第2轮：回答问题 + 给出推荐 + 等待确认（不推进流程）
# 第3轮：应用推荐 + 再次展示参数 + 等待确认
# 第4轮：进入排序选择阶段
```

### 测试用例 2: 咨询后直接确认
```python
inputs = [
    "美国，粉丝数10w-20w，找两个达人",
    "粉丝年龄有哪些选项？",
    "确认当前参数"
]

# 预期行为
# 第1轮：展示参数摘要，等待确认
# 第2轮：回答问题 + 给出推荐 + 等待确认（不推进流程）
# 第3轮：进入排序选择阶段
```

### 测试用例 3: 多次咨询后确认
```python
inputs = [
    "美国，粉丝数10w-20w，找两个达人",
    "认证类型是什么？",
    "粉丝性别有哪些选项？",
    "好的，确认"
]

# 预期行为
# 第1轮：展示参数摘要，等待确认
# 第2轮：回答问题 + 等待确认（不推进流程）
# 第3轮：回答问题 + 等待确认（不推进流程）
# 第4轮：进入排序选择阶段
```

---

## 关键修复点总结

| 问题 | 修复位置 | 修复内容 |
|------|---------|---------|
| 推荐后不知如何应用 | [agent.py:155-166](agent.py#L155-L166) | 添加明确的应用指引（"回复'应用推荐'"等） |
| 咨询时误推进流程 | [agent.py:148](agent.py#L148) | 强化"绝对禁止调用任何工具"规则 |
| 咨询时误推进流程 | [agent.py:206-210](agent.py#L206-L210) | 添加参数确认等待状态检查 |
| 咨询时误推进流程 | [agent.py:312](agent.py#L312) | 重要规则中强调"绝对禁止调用工具" |
| 咨询时误推进流程 | [agent.py:330](agent.py#L330) | 添加"必须等待用户明确确认"规则 |
| 咨询时误推进流程 | [agent.py:333](agent.py#L333) | 添加关键防御机制（自查机制） |
| 咨询时误推进流程 | [agent.py:343](agent.py#L343) | 添加参数确认阶段防御规则 |
| 状态追踪不完整 | [agent.py:44](agent.py#L44) | 添加 `waiting_for_param_confirmation` 状态 |

---

## 修复验证方法

1. **启动服务**:
   ```bash
   # 终端 1: 启动 Playwright API
   python start_api.py

   # 终端 2: 启动 Agent
   python run_agent.py
   ```

2. **测试咨询场景**:
   - 输入: "美国，粉丝数10w-20w，找两个达人"
   - 等待参数摘要展示
   - 输入: "账号类型有哪几种，认证类型是什么"
   - **验证点**: Agent 应该只回答问题，不调用 `get_max_page_number`
   - **验证点**: Agent 应该给出智能推荐并明确告知如何应用
   - **验证点**: Agent 应该等待用户确认，而不是自动进入排序阶段

3. **查看 LangSmith Trace**:
   - 访问 https://smith.langchain.com/
   - 查看最新的 Agent Run
   - **验证**: 在用户咨询时，不应该看到 `get_max_page_number` 或 `analyze_quantity_gap` 工具调用
   - **验证**: 应该在用户确认后才看到这些工具调用

---

## 后续优化建议

1. **添加状态机管理**:
   - 定义明确的状态：`INIT`, `PARAM_CONFIRMATION`, `SORTING_SELECTION`, `SCRAPING`, etc.
   - 使用状态机控制流程推进，防止跳阶段

2. **工具调用权限控制**:
   - 在不同状态下，只允许调用特定工具
   - 例如：`PARAM_CONFIRMATION` 状态下，禁止调用 `get_max_page_number`

3. **用户意图分类器**:
   - 训练一个轻量级分类器，更准确地判断用户意图
   - 分类：咨询、确认、修改、新需求等

4. **对话流程可视化**:
   - 在 CLI/Web 界面显示当前所处阶段
   - 提示用户当前可以进行的操作

---

## 修复完成时间
2025-11-07

## 修复人员
Claude Code (基于用户反馈和 LangSmith Trace 分析)
