# Logo and Payment Icons Update

## Changes Implemented
1.  **Copied Resources**:
    -   Copied `resource/logo.png` to `static/logo.png`.
    -   Copied `resource/微信支付.png` to `static/wechat_payment.png`.
    -   Copied `resource/支付宝支付.png` to `static/alipay_payment.png`.

2.  **Updated `index.html`**:
    -   Replaced the site logo reference from `logo.svg` to `logo.png`.
    -   Replaced the AliPay icon reference from `alipay.svg` to `alipay_payment.png`.
    -   Replaced the WeChat Pay icon reference from `wechat.svg` to `wechat_payment.png`.
    
## Verification
-   The files exist in the `static` directory.
-   `index.html` references the new PNG files instead of the old SVG files.
